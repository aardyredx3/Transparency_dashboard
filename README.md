# Intransparency Monitoring Dashboard

A pension-fund transparency-framework monitoring dashboard (Streamlit). Synthetic, seeded data.

## Run locally
```
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

## Deploy
Streamlit Community Cloud — main file: `app.py`.
